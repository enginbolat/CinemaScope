import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  section: {
    backgroundColor: '#111',
    borderRadius: 6,
    padding: 8,
  },
  sectionTitle: {
    color: '#61dafb',
    fontSize: 11,
    fontWeight: 'bold',
    marginBottom: 4,
    textTransform: 'uppercase',
    letterSpacing: 0.5,
  },
})
